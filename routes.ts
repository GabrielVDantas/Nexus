export const publicRoutes = [
    "/",
]

export const authRoutes = [
    "/auth/signin",
    "/auth/signup",
]

// prefixo para rotas de autenticação
export const apiAuthPrefix = "/api/auth"

export const DEFAULT_SIGNIN_REDIRECT = '/dashboard'